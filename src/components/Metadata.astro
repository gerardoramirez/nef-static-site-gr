---
interface Props {
  date?: Date;
  author?: string;
  tags?: string[];
  readingTime?: string;
}

const { date, author = 'Gerardo Ramirez', tags = [], readingTime } = Astro.props;

const formatDate = (d: Date) => {
  return new Date(d).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  });
};
---

<div class="metadata">
  {author && <span class="author">By {author}</span>}
  {date && <span class="date">{formatDate(date)}</span>}
  {readingTime && <span class="reading-time">{readingTime}</span>}
  {tags && tags.length > 0 && (
    <div class="tags">
      {tags.map(tag => (
        <a href={`/writing?tag=${encodeURIComponent(tag)}`} class="tag">#{tag}</a>
      ))}
    </div>
  )}
</div>

<style>
  .metadata {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }
  
  .author,
  .date,
  .reading-time {
    display: inline-block;
  }
  
  .author::after,
  .date::after {
    content: ' ·';
    margin-left: 0.75rem;
    color: var(--color-border-light);
  }
  
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  
  .tag {
    display: inline-block;
    color: var(--color-text-muted);
    text-decoration: none;
    font-weight: 400;
  }
  
  .tag:hover {
    color: var(--color-accent);
    text-decoration: underline;
  }
</style>

